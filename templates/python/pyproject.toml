[project]
name = "example-python-app"
version = "0.1.0"
description = "Example Python app"
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
line-length = 100

[tool.pyright]
venvPath = "."
venv = ".venv"
